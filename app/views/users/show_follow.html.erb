<div>
  <%= link_to('Back', '/users') %>
</div>

<div class="row">
  <div class="col-md-5">
    <% if @user.nil? %>
          Nu exista user-ul.
    <% else %>
      <%= render @user %>
    <% end %>
  </div>

<!--  <div class="col-md-6">-->
<!--    <h3> Relations (<%#= @relations.count %>) </h3>-->
    <%#= render @posts %>
    <%#= will_paginate(@posts, :renderer => WillPaginate::ActionView::Bootstrap4LinkRenderer, class: "pagination bg-transparent") %>
<!--  </div>-->
</div>