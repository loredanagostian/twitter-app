<%= form_with(model: current_user.follower_relationships.find_by(followed_id: @user.id),
              method: :delete, local: true) do |f| %>
  <%= f.submit 'Unfollow', class: 'btn btn-primary' %>
<% end %>